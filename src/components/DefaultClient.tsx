import React from "react";

const DefaultClient = ({ path }) => {
  return (
    <div>
      <h3> This is default page of client(using client side routes)</h3>
    </div>
  );
};

export default DefaultClient;
